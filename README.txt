This file share application is based on TCP protocol.
It can support concurrent clients using posix thread as well as 
directory broadcast and navigation is also available

In simple terms, the server acts as remote storage device while the client acts
as an application using that remote storage to download anything available on server.